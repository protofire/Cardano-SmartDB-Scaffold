import { BaseFrontEndApiCalls } from 'smart-db';
import { {{name}}Entity } from '../Entities/{{name}}.Entity';

export class {{name}}Api extends BaseFrontEndApiCalls {
    protected static _Entity = {{name}}Entity;

    // #region api

    // #endregion api
}
